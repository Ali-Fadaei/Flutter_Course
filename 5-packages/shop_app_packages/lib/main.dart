import 'package:flutter/material.dart';
import 'package:shop_app_packages/modules/app/app.dart';

void main() async {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(const App());
  // final deviceInfoPlugin = DeviceInfoPlugin();
  // var winInfo = await deviceInfoPlugin.webBrowserInfo;
}
